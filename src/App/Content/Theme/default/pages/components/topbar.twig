<nav class="navbar navbar-mycms navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar icon-one"></span>
                <span class="icon-bar icon-two"></span>
                <span class="icon-bar icon-three"></span>
            </button>
            <a class="navbar-brand" href="{{ siteURL }}">{{ siteNAME }}</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">

                {% if container.theme.is_maintenance == false %}

                    <!--{{ container.theme.get_menu }}-->
                    {{ container.plugins.applyEvent('getMenu', true) | raw }}

                {% endif %}
                {% if container.users.user_logged_in() %}
                    <ul class="nav navbar-nav navbar-right">
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">{@user_name@}
                                {@user_surname@}</a>
                            <ul class="dropdown-menu">
                                {% if container.users.isStaff %}
                                    <li>
                                        <a href="{@siteURL@}/my-admin/index">{{ container.languages.e('header_admin_a') }}</a>
                                    </li>
                                {% endif %}
                                <li><a href="{@siteURL@}/logout">{{ container.languages.e('header_logout_a') }}</a></li>
                            </ul>
                        </li>
                    </ul>
                {% else %}
                        <li><a href="{@siteURL@}/login"><i
                                        class="fa fa-user"></i> {{ container.languages.e('header_login/registration_button') }}
                            </a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>

{% if INDEX_ERROR %}
    {{ INDEX_ERROR | raw }}
    <bR>
{% endif %}